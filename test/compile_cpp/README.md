The file is generated by: `ptoas test/samples/Abs/abs-pto-ir.pto > abs_vec_core.cpp` (and some cleanup)
Note: Manually inserted #if vector statement in `abs_vec_core.cpp`

```bash
bash ./compile_lib_full_flags.sh # full options as pto-isa cmake template
bash ./compile_lib_minimum_flags.sh  # minimium options to generate so
```
